sudo: false
language: go

go:
  - tip

install:
  - go get github.com/alecthomas/gometalinter
  - gometalinter --install --update
  - go get -v -d -t ./...

script:
  - go test -v -race ./...
  - gometalinter --enable=gofmt,goimports,lll,misspell --deadline=60s
